instruction:

For this lab, the user will choose Stack-like structure and Queue-like structure to demonstrate to the user.

First, chose 1 or 2 to go to Stack-like structure mode or Queue-like mode.

Then, type 1 or 2 (push or pop value) to build up your stack or queue.


Stack-like structure means first in last out first in last out (FILO) structure.
Queue-like structure means first in first out (FIFO) structure.
